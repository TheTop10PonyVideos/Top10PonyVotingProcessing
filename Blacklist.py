# NOT WORKING CODE, PUSHED FOR AVAILABILITY BETWEEN DEVICES

from googleapiclient.discovery import build
from googleapiclient.errors import HttpError
import csv

api_key = "API KEY LOL"  # may replace this lol

csv_file = "Top_10_Pony_Videos.csv"
"""
for i in blacklist:
    for row in videos:
        if row["channel"] == blacklist:
            remove from list (ask later if with statement)
"""
